{
  "name": "pet-plugin-super-clipboard",
  "version": "1.1.0",
  "private": true,
  "description": "Official marketplace clipboard history plugin for 吐梨邦 / Desktop Pet",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "python3 scripts/build-plugin.py",
    "test": "npm run test:gate",
    "test:gate": "npm run test:plugin && npm run test:markdown && npm run test:build",
    "test:plugin": "node --test tests/plugin.test.js",
    "test:markdown": "node tests/markdown.test.js",
    "test:build": "python3 -m unittest discover -s tests -p 'test_*.py' -v"
  },
  "devDependencies": {
    "@xmldom/xmldom": "0.8.15"
  }
}
